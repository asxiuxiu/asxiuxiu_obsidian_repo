# 统一换行符为 LF，确保跨平台一致性
* text=auto eol=lf

# 确保 shell 脚本使用 LF（即使在 Windows 上）
*.sh text eol=lf

# 二进制文件不处理换行符
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.exe binary
*.dll binary

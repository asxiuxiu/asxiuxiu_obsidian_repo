---
title: 多重纹理与材质
description: Graphics Journey 阶段四第五篇。前四篇你一直在玩一张表——颜色、过滤、寻址全是为这一张表服务的。但真实的材质需要多张表同时说话：漫反射颜色、法线方向、粗糙度、金属度……本笔记回答"多张表怎么一起上"：查表插槽有数量上限、法线贴图的颜色代表的不是颜色而是方向且这个方向定义在表面自己的坐标系里、TBN 矩阵就是"表面坐标系"的基向量组、材质是"一组表的组合 + 一组常量的组合"的概念对象。最终建立"材质 = 表组 + 参数组 + 着色程序"的三元模型，为纹理系统画上句号。
date: 2026-08-04
tags:
  - graphics
  - texture
  - multi-texture
  - normal-mapping
  - TBN-matrix
  - material
  - gpu-pipeline
aliases:
  - 多重纹理
  - 材质系统
  - 法线贴图
  - TBN矩阵
---

> [[Notes/计算机图形学/纹理系统/索引|← 返回 纹理系统索引]]
> [[Notes/计算机图形学/Roadmap|← 返回 Graphics Journey 路线图]]

> **前置依赖**：
> - [[Notes/计算机图形学/纹理系统/纹理映射与UV坐标|纹理映射与UV坐标]] — 你知道纹理是 2D 查询表、UV 是逐顶点的查询坐标
> - [[Notes/计算机图形学/纹理系统/纹理对象与加载|纹理对象与加载]] — 你知道表怎么解码上传、shader 通过插槽编号间接引用表
> - [[Notes/计算机图形学/纹理系统/Mipmap与纹理过滤|Mipmap与纹理过滤]] — 你知道查表方式三个开关（过滤/选层/层间）和各向异性
> - [[Notes/计算机图形学/纹理系统/纹理寻址与采样|纹理寻址与采样]] — 你知道四种寻址模式本质是四种"表的外面是什么"的假设
> - [[Notes/计算机图形学/光照与数据流/让像素响应光|让像素响应光]] — 你知道光照公式里，像素最终颜色 = 基础颜色 × 光照结果
> - [[Notes/计算机图形学/光照与数据流/数据通道与更新频率|数据通道与更新频率]] — 你知道数据按频率分层进 GPU，本笔记往分层模型里加一行
> - [[Notes/计算机图形学/光照与数据流/法线变换的几何直觉|法线变换的几何直觉]] — 你知道法线是方向不是位置、变换规则和顶点不同
>
> **本模块要解决的像素问题**：前四篇你一直在用一张表控制每个像素的"基础颜色"。但真实的材质——砖墙的凹凸不平、生锈金属的斑驳反光、湿润地面的局部镜面——用一张颜色表根本表达不出来。首先，一张表只能给你一个颜色值，而一个像素的最终外观同时受颜色、凹凸方向、光滑程度等多个独立属性控制；其次，凹凸细节的尺度也是像素级——砖缝之间的灰泥，顶点法线只会给你一整块砖的平均朝向，做不出"缝里暗、砖面亮"的逐像素变化。**一张表不够用了：多张表怎么同时上？每张表里的数据怎么被逐像素计算正确解读？**
>
> **本模块增量**：你能说清"多张表同时上"的机制——插槽编号是桥梁、数量上限是硬约束、多张表被同一组 UV 同时索引；理解法线贴图存的不是颜色而是方向，且这个方向定义在"表面自己的坐标系"（切线空间）里——TBN 矩阵就是这个坐标系的基向量组，法线贴图蓝色的来历是"默认朝外"在这一坐标系里恰好是 (0,0,1) 映射到 RGB 的 (0.5, 0.5, 1.0)；理解"材质"是在表组 + 参数组 + 着色程序三者之上的一个概念对象，它的本质是"一组完整定义了该表面逐像素行为的数据与程序的组合"；最终建立"材质 = 表组 + 参数组 + 着色程序"的三元模型，为纹理系统画上句号。
>
> **下一步**：[[Notes/计算机图形学/纹理系统/GPU纹理压缩格式|GPU 纹理压缩格式]] — 多张表一起上的需求已经明确了，但每张表都在吃显存。一张 4K RGBA 表 64MB，PBR 材质 5 张表就是 320MB。怎么塞进显存？

---

# 多重纹理与材质

## 问题 0：一张表只给了一个颜色——但像素的外观不只有颜色

阶段四前四篇都在回答一个问题：每个像素的基础颜色从哪来。答案是"查一张 2D 颜色表"——砖墙的花纹、木箱的纹路、皮肤的底色，全是一张表搞定。到这个节点，纹理系统在单表模式下的全部机制都已就绪：表数据在显存里、查表方式四个维度（过滤/选层/层间/寻址）配好了、shader 拿着 UV 一查，像素颜色就有了。

但把这个"一张表给颜色"的模型放到真实材质面前，立刻露出两个缺口：

**缺口一：像素的外观不只有"基础颜色"这一个维度。** 阶段三的光照公式把最终颜色拆成三块：环境光（写死的低亮度）、漫反射（`N·L`）、高光（`N·H` 的幂）。漫反射和高光都依赖"这个像素看向哪个方向"——也就是法线方向。而法线来自顶点，光栅化插值到像素。一面砖墙有 100 万个像素，法线只来自几百个顶点——每个顶点一条法线，相邻顶点之间全是平滑插值。结果就是：**整面墙的法线是光滑的，灰泥缝和砖面的光照方向完全一样，没有凹凸感。**

想要灰泥缝暗一点、砖面亮一点，需要法线在**像素级**有变化——而顶点法线做不到像素级变化，正如 4.1 所说：细节的尺度远小于三角形时，顶点属性力不从心。但是，这恰恰和"基础颜色"是同构的问题——4.1 的解法是用一张表存每个像素的颜色，那**能不能也用一个表存每个像素的法线方向？**

**缺口二：~={green}一旦不止一张表了，"材质"本身变成了一个新东西。** 砖墙需要一张颜色表 + 一张法线方向表；金属可能再加一张"哪里更光滑"的表；皮肤甚至需要一张"哪里透光"的表。多张表同时参与同一个像素的计算，它们共享同一组 UV、住在不同的插槽上、被同一段逐像素计算同时查询。~={red}**"这些表是一个整体"这件事本身，需要一个名字——这个名字就是材质**=~=~。

> **本质问题：一个像素的外观由多个独立属性（颜色、法线方向、光滑度、金属度……）共同决定。每个属性都需要自己的一张表；多张表要同时挂在 GPU 上、被同一段逐像素计算、按同一组 UV 同时查询。这多张表怎么组织？每张表里的数据怎么被正确解读？这些表 + 参数 + 着色程序合在一起，形成的那个"概念对象"是什么？**

---

## 问题 1：两张表同时上——插槽编号是桥梁

4.2 已经建立了"一张表怎么挂在插槽上"的机制：绘制前把表挂到插槽 N，shader 里声明一个编号为 N 的"查表器"，逐像素阶段通过编号找到插槽上的表来查。这层间接引用的设计，当初的理由是"同一份 shader 服务任意多张表"——砖墙和木箱用同一段逐像素程序，区别只在挂哪张表。

现在需求升级成"同一段 shader **同时**服务多张表"——砖墙的一次逐像素计算，既要查颜色表（得到砖的红褐色），又要查法线方向表（知道这个像素的微表面朝哪个方向）。按同一套机制，无非是：**多占几个插槽。**

```
绘制前（CPU 侧）:
  插槽0 ← 挂上"砖墙颜色表"
  插槽1 ← 挂上"砖墙法线方向表"

逐像素阶段（GPU 侧）:
  查表器0 = 0（只是个编号）
  查表器1 = 1（只是个编号）

  基础颜色 = 查(插槽[0]上的表, uv)
  法线方向 = 查(插槽[1]上的表, uv)
```

机制上没有新东西——就是 4.2 的插槽模型做了个"乘以 N"。两组 UV 完全一样（所有表贴在同一个表面上，当然用同一组 UV），区别只在查出来的值**语义不同**：插槽 0 的值进"基础颜色"那一项，插槽 1 的值进"法线方向"那一项。

但这立刻引出一个硬约束：**插槽不是无限的。** 每个图形后端都有一个"最多同时挂多少张表"的上限。这个数量通常不大——16 到 32 个。为什么这么少？因为每个插槽背后是一整套"查表方式"的配套状态——过滤模式、寻址模式、金字塔参数——这些状态每个插槽独立维护，硬件上每一套都要占寄存器。16 个插槽意味着 16 套独立的查表方式参数常驻在逐像素阶段的硬件状态里，再多硬件成本就撑不住了。

这个上限在单表时代离你很远——你最多只用过一个插槽。但到了多表时代，它是第一个要面对的工程约束：**一个材质能用的表的张数，不能超过硬件插槽数。** 一个简单的漫反射材质用 1 张表，带了法线贴图就 2 张，PBR 材质（阶段九）轻松 5 张起步——5 张在 16 个插槽里当然够，但场景里如果有几十种材质，每种都要同时绑定自己的表组，就得频繁换挂插槽上的表。换挂本身有开销——这不是本模块的重点，但它是~={green}引擎渲染队列排序（5.6）的种子：按材质分组绘制，就是为了减少"换挂表"的次数=~。

---

## 问题 2：法线贴图——存的是方向，不是颜色

第二张表的需求说清楚了：我们需要一份"逐像素的法线方向"。但是，怎么把"方向"存进一张表里？

### naive 思路：直接存世界空间法线

最直接的方案：表里的每个纹素，存那个像素在世界空间里的法线方向——就像顶点法线一样，只是精度从"每顶点"升级到了"每纹素"。表里 (x, y, z) 三个分量各占一个通道。

这个方案在逻辑上完全正确。但有一个致命缺陷：**这张表绑定到了模型在空间中的具体朝向。** 一堵墙的砖面法线，如果存的是世界空间方向，那么墙转 90 度，所有法线方向全部作废——表里说的"朝北"此刻是"朝东"了。换一个场景、换一个朝向，就要换一张法线表。这和 UV 映射的核心设计（"同一份 UV 换一张表依然有效"）背道而驰。

### 正确的坐标系：存"相对于表面"的方向

解法是换一个参照系：法线方向不存"在世界空间里朝哪"，而是存**"在这个表面自己的坐标系里朝哪"**。

表面自己的坐标系：以表面为基准——法线方向是"朝外"，UV 的 u 方向是"朝右"，v 方向是"朝上"。在这个坐标系里，一张完全平坦的表面的所有点，法线方向都是"朝外"——也就是 (0, 0, 1)。凹凸信息要存的是**"这个点偏离了平坦表面多少"**——往左偏、往右偏、往上偏、往下偏，分别影响法线的 x 和 y 分量；z 分量始终接近 1（绝大部分表面的法线不会反过来指向内部）。

这个坐标系的本质是：**它跟着表面走。** 模型旋转、缩放、变形——坐标系自动跟着转。所以同一张法线贴图可以用在任意朝向的同一类表面上：砖墙纹理贴在水平地面、竖直墙壁、弯曲隧道，法线方向"相对于表面"的解释不变。这是法线贴图能成为通用资产的根本原因。

这个坐标系的正式名字叫**切线空间（tangent space）**。选择这个名字的原因是：构成这个坐标系的三个轴——"表面朝外"是法线方向、UV 的 u 方向是切线方向、UV 的 v 方向是副切线方向——在几何上分别是"垂直于表面"和"切于表面"的两个方向。这个命名不太直觉，但记住一件事即可：切线空间 = "以这个点的表面为基准的局部坐标系"。

### 为什么法线贴图是蓝色的

这就解释了一个经典的外观现象：法线贴图看上去是**蓝紫色**的。绝大多数纹素里，法线接近"朝外"——在切线空间里就是 (0, 0, 1)。把这个方向编码进 RGB：(0, 0, 1) → (0, 0, 255) 再归一化到 [0,1] → (0, 0, 1) → RGB 就是纯蓝色。凹凸信息让法线轻微偏离 (0, 0, 1)：偏 x 让 R 通道偏离 0.5，偏 y 让 G 通道偏离 0.5——于是蓝色基底上泛出紫红色和青绿色。

```
切线空间法线 (0, 0, 1)  → RGB (0.5, 0.5, 1.0) → 蓝色
切线空间法线 (0.3, 0, 0.95) → RGB (0.65, 0.5, 0.975) → 偏紫
切线空间法线 (-0.2, 0.1, 0.97) → RGB (0.4, 0.55, 0.985) → 偏青
```

这和三通道各占 8 位的"归一化存储"是同一种约定——4.2 的格式声明在这里是同样的机制，只是语义从"颜色"变成了"方向"。它也继承了同样的坑：Mipmap 篇（4.3）说"金字塔的正确性依赖表里是颜色"——把两个法线平均，结果是两个方向的中间方向，长度不再等于 1。短的"平均法线"在光照公式里导致高光暗淡，这不是 bug 而是近似。工程上通常接受这种近似（视觉差异很小），或者在生成金字塔时对法线做"重新归一化"——这属于"了解即可"的实现细节。

---

## 问题 3：切线空间的方向怎么跟世界空间的光源比较？——TBN 矩阵

法线方向存在切线空间里了。但光照计算里，光源方向和视线方向是在世界空间（或相机空间）里的。**两个不同坐标系里的方向不能直接做点积。**

需要一步变换：**把查表查出来的切线空间法线，转到光照计算所在的坐标系**——或者反过来，把光源方向和视线方向转到切线空间里。两种走法在数学上等价，结果一样。习惯上更常见的走法是"在切线空间做光照"——把所有方向都转到切线空间，统一计算。但概念上更容易理解的是相反的一步——"把法线转出去"——所以先用这个方向讲。

### 变换需要的三个基向量

要完成"从切线空间到世界空间"的变换，需要知道切线空间三个轴在世界空间里分别指向哪：

- **法线方向 N**：已经有了——每个顶点的法线，经过 3.3 的法线矩阵变换后，就是 N 在世界空间的方向。它是纯粹的几何语义：表面朝哪。
- **切线方向 T**：法线只告诉你"表面朝哪"，没有告诉你"表贴在表面上时，哪个方向是 u、哪个方向是 v"。切线方向就是回答这个问题的。它的完整语义和来源见下方"T 是怎么来的"——T 不是随便贴的，它由 UV 展开决定。
- **副切线方向 B**：垂直于 N 和 T，方向由右手定则决定。B 不需要单独存——逐像素计算时由 N × T 当场算出来。

这三个向量——T、B、N——构成一个 3×3 矩阵，放在一起就叫 **TBN 矩阵**。它做的事情极其单纯：

```
[ Tx Ty Tz ]   [ nx_TangentSpace ]     [ nx_WorldSpace ]
[ Bx By Bz ] × [ ny_TangentSpace ]  =  [ ny_WorldSpace ]
[ Nx Ny Nz ]   [ nz_TangentSpace ]     [ nz_WorldSpace ]

即：世界空间法线 = TBN × 切线空间法线
```

把法线贴图查出来的 (R, G, B) 先解归一化变成 (-1, 1) 范围的方向向量，再乘上 TBN 矩阵，就得到了在世界空间里的法线方向。这个方向可以直接拿去和世界空间的光源方向、视线方向做点积，光照计算照旧。

### T 是怎么来的——它的语义被 UV 展开决定

T 不是随便选的，也不是一个纯几何概念。它的完整语义是：

> **T = "在这个顶点处，UV 的 u 坐标增大的方向，在 3D 世界里指向哪"**

换句话说，T 回答的问题是："当法线贴图朝某个方向凹凸时，我该把贴图的 u 轴对齐到模型表面的哪条线上？"——它是把**贴图的二维坐标系搬到 3D 表面上的那根"对齐旋转针"**。

为什么 T 必须由 UV 展开决定，而不是"贴个表面的方向"就行？因为法线贴图和颜色贴图一样，用**同一组 UV** 去采样（问题 1 说所有表用同一组 UV）。那么法线贴图里的 u 方向，就自动跟着"你采样时 u 增大的方向"走——也就是 UV 展开决定的方向。所以：

- 表面几何一模一样（同一个顶点、同一条法线），**换个 UV 展开方式，T 就完全不同**；
- 法线贴图里的纹素读到的是"偏移方向"，做点积时它必须知道"贴图的 u/v 各朝 3D 空间的哪边"——这个信息 T（和 B）负责提供。

建模软件在导出模型时，从三个顶点的**位置差**和**UV 差**反解出 T（标准算法就是参考资源里的 MikkTSpace）。所以 T 是**逐顶点的属性**，和位置、法线、UV 一样走顶点缓冲进 GPU，光栅化时插值到每个像素。

### T 的特殊之处：它是"几何 × 贴图映射"的产物

把三个基向量摆在一起看，能看出 T 和 N 的本质区别：

| 向量 | 语义 | 来源 | 是不是纯几何 |
|------|------|------|------|
| **N** 法线 | 表面朝外的方向（垂直表面） | 建模的几何语义 | 是 |
| **T** 切线 | UV 的 u 方向在 3D 里指向哪 | **由位置 + UV 映射算出** | **不是——还绑定贴图展开** |
| **B** 副切线 | 补成第三个轴 = N × T | 逐像素现算，不存 | — |

T 是三者里唯一一个**不是纯几何、而是"几何 × 贴图映射"的产物**——同样一个顶点，换张法线贴图、换个 UV 展开方向，T 就得跟着变。这也是为什么你光看表面形状猜不出 T，得看这张图是怎么摊上去的。

### 直觉化：一个圆球和一道焊缝

一个光滑圆球，几何上完全球对称，每个顶点的法线都是"朝外"，给 N 就够。但你在它上面贴一道法线贴图做的**横向焊缝**——焊缝在贴图里沿 u 方向排布。问题来了：这道焊缝在圆球上**朝哪个方向绕**？

- 想让焊缝沿"赤道方向"绕一圈 → UV 的 u 要对齐赤道（经度方向）；
- 想让它围着一个点打转 → u 得对齐纬线方向（经度递增的方向）。

同一个圆球、同一张法线贴图，**T 不同，焊缝在 3D 表面上的走向就完全相反。** 这个例子说明：T 是从贴图坐标系到 3D 坐标系的那根"对齐旋转针"，它必须逐顶点存——因为表面是弯的，每个顶点处"u 增大的方向"各不相同。

一句话总结 T 的语义：**顶点切线 T = "贴图的 u 方向在 3D 表面局部朝向哪"，它由 UV 展开决定、逐顶点存储、和顶点法线 N 一起构成 TBN，让法线贴图里的每个方向能转出到光照空间比较。**

这里有一个微妙的精度问题：插值后的 T 和 N 不一定完美垂直——这是浮点运算可容忍的误差。真正重要的是：插值后的 T、B、N 在大多数像素上仍然近似构成一个正交坐标系。对于需要严格正交的场景（比如某些各向异性光照模型），可以在逐像素阶段对 T 做一次施密特正交化——去掉 T 中与 N 平行的分量，重新归一化。这一步的影响在绝大多数材质上肉眼不可见，属于"遇到奇怪高光偏移时知道去哪查"的知识。

### 另一种走法：把光照转到切线空间

TBN 的逆矩阵（在正交时就是转置）可以把世界空间的方向转到切线空间。走这条路的动机很简单：把变换放在逐顶点阶段做。顶点着色器里，每个顶点把光源方向和视线方向转进切线空间；光栅化把变换后的方向插值到每个像素；片段着色器里直接用法线贴图的原始值做点积——省去了每个像素做一次矩阵乘法的开销。

两种走法的选择是性能 trade-off，不影响正确性。学习阶段哪个直觉更顺走哪个；引擎里通常选"在切线空间做光照"以节省逐像素矩阵乘法。

---

## 问题 4：当表多了，"材质"这个概念就浮出水面了

两张表的需求跑通了：插槽编号隔开，同一份 shader 同时查两张表，颜色给漫反射、法线给光照方向。但如果再往上加——粗糙度贴图（哪里光滑哪里粗糙）、金属度贴图（哪里是金属哪里是电介质）、环境光遮蔽贴图（哪里天生就暗）——shader 里会多出一堆"查表器"变量。每种材质需要的表的组合不同：砖墙要颜色 + 法线，生锈铁板要颜色 + 法线 + 粗糙度 + 金属度，发光广告牌只要颜色 + 自发光强度。

于是出现了一个概念对象：**材质（Material）**。它不是某一张表、也不是某一段着色程序，而是**"一组表的组合 + 一组常量的组合 + 一段着色程序"的合体**。

```
材质 "砖墙" = {
  表组:   插槽0 ← 砖墙颜色表
          插槽1 ← 砖墙法线方向表
  常量组: 粗糙度 = 0.9（粗糙）
          金属度 = 0.0（非金属）
  着色程序: StandardLit（标准光照）
}

材质 "生锈铁板" = {
  表组:   插槽0 ← 铁锈颜色表
          插槽1 ← 铁锈法线方向表
          插槽2 ← 粗糙度变化表
          插槽3 ← 金属度变化表
  常量组: （无——这些属性全由表提供，逐像素变化）
  着色程序: StandardLit
}
```

注意"粗糙度"在两个材质里以不同形式出现：砖墙里它是一个全局常量（整面墙都粗糙），铁锈里它是一张表（有的地方锈得粗糙、有的地方还光滑）。这就是 3.2 的频率分层模型在材质定义里的应用：**同一个语义属性，更新频率可以不同**。"全局常量"适合均匀材质；"查表"适合空间变化的材质。材质系统要同时支持两种，而且允许它们无缝替换——"这个槽位要么给一张表、要么给一个常量"是引擎材质系统的基本能力。

### 表的可选性

并非每种材质都需要填满所有语义槽。没有法线贴图的材质，shader 里就拿顶点插值法线来算——光照公式不变，只是法线的来源从"逐像素查表"降级为"逐顶点插值"。同样，没有粗糙度贴图就用常量粗糙度。材质的每个表槽都是**可选的**，缺省时回退到一个合理的常数值。

这意味着：同一段着色程序，要能正确处理"某张表有、某张表没有"的情况。这是着色器变体（shader variant）问题的起点——"是否有法线贴图"这个布尔条件决定了编译时是否包含查法线表的代码。这个问题的工程解法（宏定义变体、Uber Shader、运行时编译缓存）是阶段十二的内容，此刻只需建立直觉：**多表材质制造了着色程序的分叉，分叉需要管理。**

### 材质 + 表组 + 着色程序 = 三元模型

总结本模块建立的材质概念：

```
材质
├── 着色程序 ── 决定"怎么算"（光照模型、BRDF）
├── 表组     ── 决定"每个像素的各个属性是多少"（颜色/法线/粗糙度/金属度...）
└── 常量组   ── 决定"所有像素共享的参数是多少"（全局粗糙度、颜色常量...）
```

这三个组成部分各自独立变化：同一段着色程序（标准光照）可以搭配完全不同的表组+常量组（砖墙、铁锈、皮肤）；同一张颜色表也可以搭配不同的着色程序（Blinn-Phong 和 PBR 算出来的画面不同，但底图是同一张）。**表组负责采样的"是什么"、着色程序负责采样的"怎么算"、常量组负责采样的"统一修正"**——三者解耦是引擎材质系统的第一条设计原则。

---

## 问题 5：回顾——材质 "表组" 里的几张表在数据通道分层中的位置

3.2 建立了数据通道的频率分层模型，4.2 往里加了"查询表"这一行：

| 通道 | 更新频率 | 数据量级 | 代表数据 |
|---|---|---|---|
| 顶点属性 | 每顶点 | 每顶点几 float | 位置、法线、UV、**切线 T** ← 新增 |
| 插值传递 | 每像素（来自顶点） | — | 插值后的 UV、法线、**T、B** ← 新增 |
| 常量 | 每物体/每帧 | 几十~几百字节 | MVP、光源参数、**材质常量（粗糙度、金属度等）** ← 新增 |
| 查询表 | 资源级（几乎不变） | MB 级，插槽间接引用 | 颜色表、**法线方向表、粗糙度表、金属度表……** ← 新增 |

变化有三处：
1. 顶点属性多了一个 T（切线方向），它是 TBN 矩阵所需的逐顶点数据；
2. 插值通道多了 T 和 B（B = N × T 在逐像素阶段现算），它们和法线一起构成每个像素的 TBN 矩阵；
3. 查询表和常量各自多了语义槽位——查询表从"一张颜色表"变成"一组多张语义表"，常量从"MVP + 光源"变成"MVP + 光源 + 材质参数"。

这些变化都是"加了新数据"而非"改了旧机制"——插槽模型、频率分层、独立决策，全部复用阶段二~四已建立的框架。

---

## 问题 6：法线贴图的 Mipmap 陷阱——为什么 4.3 说过"金字塔的正确性依赖表里是颜色"

法线贴图也需要金字塔——远处的表面同样有法线走样的问题。但 4.3 明确警告过：**金字塔假设"平均是有意义的"**，而法线是方向——把两个方向"平均"出来的第三个方向，长度不再等于 1。

```
两个法线的算术平均：
  (0.7, 0, 0.7) 平均 (0, 0.7, 0.7) = (0.35, 0.35, 0.7)
  长度 ≈ 0.87 ≠ 1.0

在光照公式 N·L 里，N 的长度直接影响结果亮度——
  一个"变短"的法线让光照比正常情况更暗。
```

这是法线贴图 + 金字塔的已知"近似"——几乎所有实时渲染都接受它，因为视觉差异通常被漫反射和高光的其他因素掩盖。需要精确结果的场景（比如汽车漆面），可以在生成金字塔后对每一层的每个纹素**重新归一化**法线。这个操作的代价在离线烘焙时支付，不影响运行时性能——这是 4.6 压缩纹理管线里会再见面的话题。

还有一种选择：不用金字塔，但这样远处表面的法线走样（闪烁）会回来。两害相权：略微暗淡的间接光照 << 可见闪烁。工程结论是：**法线贴图一样开金字塔，接受微小的间接光照误差。**

---

## 收尾落地：多表绑定与材质在各图形后端中怎么表达

> 推导到此结束——你已经知道：多张表通过多占插槽同时挂载，shader 通过多个查表器变量引用不同编号的插槽；法线贴图存的是切线空间里的方向而非颜色，TBN 矩阵负责把切线空间法线转到光照空间；材质 = 表组 + 常量组 + 着色程序的三元合体。下面是各后端的落地，纯文字、无代码，具体实现交给 AI 生成。

- **OpenGL**：插槽在 OpenGL 里叫"纹理单元"，硬件保证至少 16 个，现代硬件通常 32~80 个，可按需查询上限。绑定流程是：激活插槽 N → 把表挂上去 → 给 shader 里的查表器变量赋值编号 N。这套 API 的"激活-绑定-赋值"三步恰好对应 4.2 的"插槽编号是桥梁"的模型。法线贴图没有专门的 API 对象——它就是一张普通的 2D 表，存在普通纹理对象里；TBN 矩阵没有专门的 API 对象——T 是一个普通的顶点属性，N 也是，逐像素的 B = cross(N, T) 在 shader 里手写一行。材质在 OpenGL 里没有原生概念——表组、常量组、着色程序三者全靠你的代码组织。

- **Vulkan**：插槽在 Vulkan 里通过"描述符集"来表达——一个描述符集是一次绑定的"一批资源"（几张表 + 几块常量缓冲），一个材质就是一个或一组描述符集。设计意图是：把"一组表的组合 + 一组常量的组合"做成一个显式对象，绑定一次就全部到位——这和本模块的"材质 = 表组 + 常量组"模型完全一致。插槽上限是物理硬件的限制，在 Vulkan 里对应的上限更高更灵活（描述符集可以很大，也有无绑定扩展来规避上限）。TBN 同样没有专门对象，T 是顶点属性、N 是顶点属性、B 在 shader 里算——和 OpenGL 一样。

- **D3D12**：与 Vulkan 同路——描述符表绑定一批资源（SRV 描述符指向表、CBV 描述符指向常量缓冲），材质 = 一套描述符表的组合。根签名定义了"shader 期望哪些槽位"，相当于"材质的插槽模板"。

- **Metal**：纹理通过参数缓冲区或逐个绑定到着色器参数上，材质的概念在 Metal 里对应"参数缓冲区 + 渲染管线状态 + 一组纹理"的组合——和三元模型结构一致。

一句话对照：**"多张表 + 多常量 + 着色程序"在概念上是同一个东西（材质），但 OpenGL 把它们分散在三个独立的 API 对象里靠代码逻辑组合，现代 API 用描述符集/描述符表把它们显式打包——打包的理由和"渲染状态一次验证"一样：CPU 预验证、批量绑定、减少驱动内部的状态散落。**TBN 矩阵没有一个 API 有专门对象——它太基础了，就是三个顶点属性 + 一个逐像素向量叉积，不值得一个 API 抽象。

---

## 总结：这篇笔记建立的核心认知

1. **多表同时上的机制就是"多占几个插槽"**——机制上没有新东西，但引出第一个硬约束：插槽有数量上限。一个材质能用的表的张数不能超过硬件上限；场景中的几十种材质需要按材质分组绘制以减少"换挂插槽"的开销。

2. **法线贴图存的是切线空间里的方向，不是颜色**——切线空间就是把法线定义在"表面自己的坐标系"里，这样同一张法线贴图可以用在任意朝向的表面上。法线贴图普遍呈蓝色的原因是：切线空间里的"朝外"方向 (0,0,1) 编码到 RGB 就是 (0.5, 0.5, 1.0)。

3. **TBN 矩阵就是把切线空间的方向转到光照空间的变换矩阵**——三个基向量：T 沿 UV 的 u 方向、B 沿 UV 的 v 方向（B = N × T）、N 垂直于表面。其中 **T 不是纯几何向量，而是"几何 × UV 映射"的产物**——它由顶点的位置差和 UV 展开反解出来，语义是"贴图的 u 方向在 3D 表面局部指向哪"，同一个几何点换个 UV 展开 T 就不同；B 在逐像素阶段当场算。切线空间法线 × TBN = 可以参与光照计算的法线。

4. **材质 = 表组 + 常量组 + 着色程序的三元合体**——表组决定"每个像素各个属性的值"，常量组决定"所有像素共享的参数"，着色程序决定"怎么算"。三元独立变化、彼此解耦。

5. **法线贴图的金字塔可以开，接受微小误差**——把两个法线平均会缩短结果向量，导致间接光照略微变暗；但在所有工程场景里，这个误差远小于关掉金字塔带来的法线走样（闪烁）。

6. **本模块是阶段四"查表方式"四维度的总回收终点**——从 4.1 提出"数据和查法可分"，到 4.3/4.4 说清"查法"的三个开关和一个寻址维度，到本模块说清"多张表一起上的组织方式"——纹理系统在单 Pass 世界里的全部机制到此齐备。后面两篇（4.6 压缩格式、4.7 Cubemap/纹理数组）是"成组的纹理"在工程资源管线和特殊使用场景（天空盒、粒子）里的展开，不改变本模块建立的"材质 = 表组 + 参数组 + 着色程序"的核心结构。

---

> **参考资源**：
> - LearnOpenGL > Advanced Lighting > Normal Mapping：法线贴图的切线空间解释与 TBN 矩阵推导，与本篇问题 2~3 完全对应
> - [The Book of Shaders: Normal Maps](https://thebookofshaders.com/)：法线贴图的 shader 实现与可视化演示
> - Real-Time Rendering 第四版，第 6.7 节（Bump Mapping）：从凹凸贴图到法线贴图到视差贴图的演进路线，及 TBN 矩阵的数学推导
> - [OpenGL Tutorial: Normal Mapping (ogldev)](https://ogldev.org/www/tutorial26/tutorial26.html)：T 的计算（从三角形边和 UV 差推导切线），本模块未展开的数学推导见此处
> - [Lengyel, "Computing Tangent Space Basis Vectors for an Arbitrary Mesh"](http://www.terathon.com/code/tangent.html)：任意网格切线空间基向量的标准算法（MikkTSpace），工业界事实标准
> - [MikkTSpace](http://www.mikktspace.com/)：由 Morten S. Mikkelsen 提出的切线空间计算约定，被 Unity/Unreal/Blender 等广泛采用——不同工具生成的法线贴图能通用，靠的就是这个约定
>
> [[Notes/计算机图形学/Roadmap|← 返回 Graphics Journey 路线图]]

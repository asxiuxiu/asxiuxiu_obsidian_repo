---
title: 字符串系统
date: 2026-05-08
tags:
  - self-game-engine
  - foundation
  - string
aliases:
  - String System
---

> **前置依赖**：[[引擎基础类型与平台抽象]]、[[数学基础]]
> **本模块增量**：为引擎提供高性能字符串表示（SSO、StringId）和零分配格式化能力，使日志、资源查找、调试输出不再成为帧率瓶颈。
>
> 本笔记是字符串系统的**总览与导航**。四个子话题的深度剖析分别写在以下笔记中：
> - [[可变字符串与 SSO]] — SSO 原理与三种工业级实现的精确内存布局、StringView 设计、COW 历史与废弃
> - [[字符串标识与 InternPool]] — 字符串哈希算法选择、UE FNamePool 的工业级架构（分块/无锁/TLS）
> - [[字符串格式化]] — ADL 自由函数栈格式化器、fmtlib 编译期解析与类型擦除、C++20 std::format
> - [[日志消息与字符串系统的边界]] — 日志消息虽然经字符串格式化器生产，但存储架构与标识符完全不同，需专用环形缓冲区

# 字符串系统

## 为什么需要自定义字符串系统？

`std::string` 对引擎热路径来说有三个致命问题：

1. **小分配泛滥**：80% 的引擎**标识符字符串**（组件名、资源名、配置键）长度 < 16 字节，但 `std::string` 全部走堆分配。日志消息等动态长文本不在此列，详见 [[日志消息与字符串系统的边界]]
2. **Cache 不友好**：字符数据在堆的另一处，遍历时频繁解引用
3. **比较成本高**：O(n) 逐字节比较在组件查找、事件匹配时被放大成千上万倍

自定义字符串系统通过三种专用类型分别解决这三个问题：

| 问题 | 解决方案 | 所在笔记 |
|------|---------|---------|
| 小分配泛滥 | SSO `String`（短字符串内联，消除堆分配） | [[可变字符串与 SSO]] |
| 比较成本高 | `StringId`（编译期哈希 + Intern 池，O(1) 整数比较） | [[字符串标识与 InternPool]] |
| 格式化开销大 | 栈格式化器（零堆分配、类型安全、ADL 扩展） | [[字符串格式化]] |

## 默认推荐路径

| 用途 | 推荐类型 | 理由 |
|------|---------|------|
| 动态文本（UI、错误信息、日志内容） | SSO `String` | 小字符串无堆分配、支持修改、可接自定义分配器 |
| 只读字符串参数（函数参数） | `StringView` | 零拷贝、无分配、可接受字面量/String/char* |
| 资源路径/组件名/配置键/事件名 | `StringId` | O(1) 比较、确定性、可序列化、Intern 池保证唯一性 |
| 日志/调试格式化输出 | 栈格式化器 / `std::format`（C++20） | 生产文本用栈格式化器，**存储文本用环形缓冲区**——日志消息不走 `String`。详见 [[日志消息与字符串系统的边界]] |
| 本地化管理 | 暂不实现 | 单机原型不需要，阶段 7 工具链再扩展 |

**核心原则**：引擎中的字符串不是单一数据结构，而是**根据用途分层**——可变文本用 `String`，标识比较用 `StringId`，只读引用用 `StringView`，格式化输出用栈格式化器。

## 引擎对照：各引擎的字符串策略

| 引擎 | 可变字符串 | 字符串标识 | 格式化系统 | 核心洞察 |
|------|-----------|-----------|-----------|---------|
| **UE** | `FString`（TArray 封装，无内部 SSO，用 `TInlineAllocator` 实现类似效果） | `FName` + `FNamePool`（分块分配、无锁读取、TLS 批量提交、大小写不敏感） | `FString::Printf`（C 风格），自定义类型用 `LexToString` | 三级分层：FString（可变）→ FName（标识）→ FText（本地化） |
| **chaos** | 无突出的自定义 String（可能复用 std::string） | 不详 | 自研 `StringFormat`（`{index:align.width.type}` 语法，`alloca` + 宏注册） | 格式化系统的模块边界设计优秀 |
| **Bevy** | Rust `String`（有 SSO 但非 union 方式） | **不用字符串标识组件**（用 `TypeId`），资源用 `Handle<A>` | Rust `Display` trait | ECS 架构下"不用字符串解决问题"——组件类型、资源引用、系统标识全部用整数 ID |

**默认推荐**：在 ECS 兼容前提下，吸收 UE 的"字符串按用途分层"核心洞察，但用 `String`（libc++ 风格 24B SSO）代替 `FString`，用 `StringId`（简化版 FNamePool）代替 `FName`，用 `std::format`（C++20）或 fmtlib 代替 UE 的 C 风格格式化。

## ECS 映射：字符串在 ECS 中该怎么放？

```cpp
// ❌ 反模式：组件存储 String
struct NameComponent {
    String name; // 24 字节对象，可能含堆指针 → 破坏密集数组
};

// ✅ 正确：组件存储 StringId，System 参数用 StringView
struct NameComponent {
    StringId nameId; // u64，纯值，无指针
};

// 实际字符串内容存在 ECS Resource（全局状态）中
struct NameRegistry : public ECSResource {
    StringInternPool pool;
    HashMap<Entity, StringId> entityNames;
};
```

**核心原则**：ECS 组件中只存 `StringId`（u64），不存字符串对象。所有字符串内容上浮到 Resource 或外部系统中。

### 从 OOP 到 ECS 的重构映射

| OOP 设计 | ECS 重构 |
|---------|---------|
| Actor 的 Name 成员（FString） | `NameComponent { StringId nameId }` + `NameRegistry` Resource |
| 类型名字符串（如 UClass::GetFName） | `ComponentId`（u64）+ `TypeRegistry`（编译期注册） |
| 资源路径（FString 路径） | `AssetPathId`（StringId）+ `AssetRegistry` Resource |
| 日志 CategoryName（FName） | `LogCategoryId`（StringId），编译期常量 |

---

## AI 友好设计红线

| 检查项          | 结论                         | 说明                                                 |
| ------------ | -------------------------- | -------------------------------------------------- |
| **状态平铺**     | ✅ StringId 可直接作为 ECS 组件    | 8 字节纯值，无指针，序列化/拷贝 trivial                          |
| **自描述**      | ✅ AI 可通过 TypeRegistry 反查名称 | Debug 构建维护 `StringId → const char*` 映射；Release 可剥离 |
| **确定性**      | ✅ FNV-1a 哈希是纯算法            | 给定相同输入，输出始终相同。支持快照保存与回放                            |
| **工具边界**     | 应操作 `StringId`             | AI 修改资源名或组件名时，优先操作 `StringId`，原始字符串仅作为初始注册输入       |
| **Agent 安全** | ⚠️ 需防范哈希冲突                 | Debug 构建启用 intern 池碰撞检测；关键路径（如资源加载）使用二次校验          |

---

## 回探替换清单

本模块的产出需要回探替换阶段 1 和阶段 2 中的临时实现：

| 临时实现（阶段 1/2） | 替换为（阶段 3.3） | 验证方法 |
|---------------------|-------------------|---------|
| 日志系统中的 `std::string` 日志内容 | 栈格式化器 + 环形缓冲区 | 日志面板正常滚动，Profiler 显示分配次数下降 |
| 阶段 2 的组件名硬编码字符串（如 `"Transform"`） | `StringId` 编译期常量 | Inspector 仍能显示组件标签，且切换为 StringId 后无性能回退 |
| 日志格式化中的 `std::ostringstream` | 栈格式化器 / `std::format` | 输出格式一致，无堆分配 |
| 函数参数中的 `const std::string&` | `StringView` | 消除隐式构造开销 |

---

## 扩展路径（更极端工业场景）

- **无锁 Intern 池**：用 TLS 线程本地缓冲 + 批量提交消除多线程 `intern()` 的锁竞争。详见 [[字符串标识与 InternPool]]
- **大小写不敏感 FName**：UE 的 `FName` 默认大小写不敏感，适合资源路径比较。当前实现是大小写敏感的，可在 Intern 池中加入可选的 case-insensitive 模式
- **本地化与富文本**：引入 `FText` 级别的本地化支持（翻译表、文本收集、复数规则），以及 UI 富文本格式化（颜色、字体、超链接）
- **字符串压缩**：对序列化后的字符串数据做字典压缩（如 Zstd），减少存档和网络包体积

---

> **下一步**：[[内存分配器]] — 为 SSO String 的堆分配路径、InternPool 的分块分配、格式化器的动态缓冲提供自定义分配策略，彻底摆脱 `new/delete` 的不可控性。
>
> 字符串系统的四个子话题已分别在以下笔记中深入展开：
> - [[可变字符串与 SSO]]
> - [[字符串标识与 InternPool]]
> - [[字符串格式化]]
> - [[日志消息与字符串系统的边界]]

---
title: 从零开始的引擎骨架
date: 2026-04-10
tags:
  - self-game-engine
  - architecture
  - foundation
  - ECS
  - agent
aliases:
  - 引擎骨架
  - engine-skeleton
---

> [← 返回 SelfGameEngine 索引]([[00-索引|SelfGameEngine 索引]])

# 从零开始的引擎骨架

## Why：为什么需要先搭骨架，而不是直接写渲染？

很多人开始做引擎时，第一步是创建一个窗口、初始化 Vulkan、在屏幕上画一个三角形。这很爽，但它只是一个渲染 demo。

而如果你的目标是 **vibe coding**——让 AI 能够长时间无人干预地帮你构建、调试、迭代这个引擎——那么第一步不应该是"让画面出来"，而应该是**让 AI 能看懂和操作系统**。

### 没有骨架时，世界有多糟

1. **AI 只能写代码，不能验证结果**
   - 你让 AI "把玩家移动速度减半"，它改了一个深层继承树的基类字段，但运行时这个字段根本不会被读取。
   - 因为引擎状态是一片黑盒：对象图深埋指针、副作用散落在虚函数表里，AI 无法"看到"修改后的真实效果。

2. **每次调试都需要你亲自当翻译**
   - 运行时崩溃，AI 拿到了一份调用栈文本，却不知道内存中真正发生了什么。
   - 你需要自己打开调试器、描述内存布局——AI 的自主行动半径被限制在"写代码→等你编译→你告诉它结果"。

3. **多个 Agent 无法并行工作**
   - 一个 Agent 负责物理调参，另一个负责 gameplay 平衡，但引擎没有提供状态隔离和工具边界。
   - 两个 Agent 的改动互相覆盖，最终只能串行执行，失去了 multi-agent 的协作优势。

### 一个 AI 友好的骨架解决什么

| 问题 | 骨架的答案 |
|------|-----------|
| AI 怎么知道某个对象有哪些属性？ | 自描述的类型注册表（TypeRegistry），运行时可直接查询 |
| AI 怎么修改状态而不引发副作用？ | 平铺的 ECS 组件数据，所有状态都是显式的、可序列化的 |
| AI 怎么验证"改完对不对"？ | 确定性 Tick + 快照回放：保存状态 → 改动 → 对比 diff |
| 多个 Agent 怎么同时工作？ | AgentBridge 工具边界 + 组件白名单：每个 Agent 只能操作被授权的组件 |
| 新系统怎么接入？ | 分阶段 Tick + 依赖排序：系统以纯函数方式注册到调度器 |

> **核心结论**：这个骨架不是为了"优雅"，而是为了让你在 6 个月后，能让一个 AI Agent 自主地在这个引擎里添加新系统而不至于把现有逻辑搞崩。

---

## What：最小能跑的 AI 友好引擎骨架长什么样？

下面的代码是一个**真正可以编译运行**的极简引擎骨架。它不包含任何图形 API，但已经具备了 AI 友好引擎最核心的四个能力：
1. **平铺 ECS 数据层**：所有状态对 AI 透明
2. **自描述 TypeRegistry**：AI 无需头文件即可理解数据结构
3. **确定性 Tick 调度**：同样的输入一定得到同样的状态
4. **AgentBridge 工具边界**：AI 通过结构化接口操作引擎

```cpp
#include <cstdint>
#include <vector>
#include <string>
#include <unordered_map>
#include <functional>
#include <iostream>
#include <cassert>

// ============================================================
// 1. 平铺 ECS 数据层
// ============================================================
using Entity = uint32_t;
constexpr Entity MAX_ENTITIES = 1024;

struct Position { float x{0}, y{0}, z{0}; };
struct Velocity { float x{0}, y{0}, z{0}; };

class World {
public:
    std::vector<Position> positions;
    std::vector<Velocity> velocities;
    std::vector<bool>     alive;

    World() {
        positions.resize(MAX_ENTITIES);
        velocities.resize(MAX_ENTITIES);
        alive.resize(MAX_ENTITIES, false);
    }

    Entity spawn() {
        for (Entity e = 0; e < MAX_ENTITIES; ++e) {
            if (!alive[e]) { alive[e] = true; return e; }
        }
        assert(false); return 0;
    }
    void destroy(Entity e) { alive[e] = false; }
    bool valid(Entity e) const { return e < MAX_ENTITIES && alive[e]; }
};

// ============================================================
// 2. 自描述类型注册表（AI 的眼睛）
// ============================================================
enum class FieldType { Float };
struct FieldDesc { std::string name; FieldType type; size_t offset; };
struct ComponentDesc {
    std::string name;
    std::vector<FieldDesc> fields;
    size_t size;
};

class TypeRegistry {
public:
    std::unordered_map<std::string, ComponentDesc> components;

    void register_all() {
        components["Position"] = {
            "Position",
            { {"x", FieldType::Float, offsetof(Position, x)},
              {"y", FieldType::Float, offsetof(Position, y)},
              {"z", FieldType::Float, offsetof(Position, z)} },
            sizeof(Position)
        };
        components["Velocity"] = {
            "Velocity",
            { {"x", FieldType::Float, offsetof(Velocity, x)},
              {"y", FieldType::Float, offsetof(Velocity, y)},
              {"z", FieldType::Float, offsetof(Velocity, z)} },
            sizeof(Velocity)
        };
    }
};

// ============================================================
// 3. 确定性 System 调度
// ============================================================
class System {
public:
    virtual ~System() = default;
    virtual void tick(World&, float dt) {}
};

class MovementSystem : public System {
public:
    void tick(World& world, float dt) override {
        for (Entity e = 0; e < MAX_ENTITIES; ++e) {
            if (!world.alive[e]) continue;
            world.positions[e].x += world.velocities[e].x * dt;
            world.positions[e].y += world.velocities[e].y * dt;
            world.positions[e].z += world.velocities[e].z * dt;
        }
    }
};

class Scheduler {
    std::vector<std::unique_ptr<System>> systems;
public:
    void add(std::unique_ptr<System> sys) {
        systems.push_back(std::move(sys));
    }
    void tick(World& world, float dt) {
        for (auto& sys : systems) {
            sys->tick(world, dt);  // 确定性：固定顺序，无隐藏状态
        }
    }
};

// ============================================================
// 4. AgentBridge 工具边界（AI 的手）
// ============================================================
struct ToolResult { bool ok; std::string json; };

class AgentBridge {
    World& world;
    TypeRegistry& registry;
    Scheduler& scheduler;
public:
    AgentBridge(World& w, TypeRegistry& r, Scheduler& s)
        : world(w), registry(r), scheduler(s) {}

    // Tool: 查询带有某组件的所有实体
    ToolResult query_entities(const std::string& comp) {
        if (!registry.components.count(comp))
            return {false, R"({"error":"unknown component"})"};
        std::string json = "[";
        for (Entity e = 0; e < MAX_ENTITIES; ++e) {
            if (!world.alive[e]) continue;
            if (json.size() > 1) json += ",";
            json += std::to_string(e);
        }
        json += "]";
        return {true, json};
    }

    // Tool: 修改实体的 Velocity
    ToolResult set_velocity(Entity e, float vx, float vy, float vz) {
        if (!world.valid(e))
            return {false, R"({"error":"invalid entity"})"};
        world.velocities[e] = {vx, vy, vz};
        return {true, R"({"status":"ok"})"};
    }

    // Tool: 推进一帧
    ToolResult step(float dt) {
        scheduler.tick(world, dt);
        return {true, R"({"status":"tick_complete"})"};
    }
};

// ============================================================
// 主循环：AI Agent 完全通过 Tool 接口驱动引擎
// ============================================================
int main() {
    World world;
    TypeRegistry registry; registry.register_all();
    Scheduler scheduler;
    scheduler.add(std::make_unique<MovementSystem>());
    AgentBridge bridge(world, registry, scheduler);

    Entity player = world.spawn();
    bridge.set_velocity(player, 1.0f, 0.0f, 0.0f);

    // Agent 决定推进 3 帧并观察结果
    for (int i = 0; i < 3; ++i) {
        bridge.step(0.016f);
        auto* t = &world.positions[player];
        std::cout << "Frame " << i << ": pos = (" << t->x << ", " << t->y << ")\n";
    }
    return 0;
}
```

### 这个最小实现已经解决了什么

| 能力 | 说明 |
|------|------|
| **状态完全平铺** | `World` 里的所有数据都是连续数组，AI 可以直接序列化、diff、分析 |
| **零指针追踪** | 没有深层对象树、没有虚函数副作用，AI 不需要"猜"引用关系 |
| **运行时自描述** | `TypeRegistry` 让 AI 知道每个组件有哪些字段、偏移量和类型 |
| **确定性执行** | `scheduler.tick(world, dt)` 是纯函数式调用，输入固定则输出固定 |
| **工具边界清晰** | AI 不直接生成代码，而是通过 `AgentBridge` 调用结构化工具 |

### 这个最小实现还缺什么

- **没有自动化反射**：`TypeRegistry` 还是手写的，真实引擎需要宏或代码生成
- **没有系统依赖排序**：`PhysicsSystem` 必须在 `MovementSystem` 之前执行时，当前代码无法表达
- **没有增量变更日志**：AI 每次都要全量扫描世界才能知道发生了什么变化
- **没有快照与回滚**：AI 还不能"尝试一个改动，不满意就撤销"
- **没有 Agent 权限隔离**：任何 Agent 都能改任何组件

这些不是缺陷，而是**下一阶段的明确目标**。

---

## How：真实引擎的骨架是如何复杂起来的？

### 阶段 1：能用 —— 解决"AI 能看见状态"

#### 触发原因

当你的引擎从 1 个系统变成 10 个系统时，AI 开始迷失：
- "`Player` 类继承自 `Actor`，`Actor` 里有一个 `transform_` 指针……" AI 花了半天才追踪到真正的状态位置。
- 你让 AI "找出所有速度大于 100 的物体"，它需要遍历五层继承树才能确认哪些对象有速度属性。

#### 代码层面的变化

**A. 引入 ECS 强制所有状态平铺**

这是本知识库的默认假设。即使你的源码分析对象是 OOP 引擎，在通用知识中也要用 ECS 重写。因为 ECS 的"数据与逻辑分离"天然契合 AI 的批量分析模式。

**B. 手写 `TypeRegistry` → 自动化反射生成**

```cpp
// 阶段 1：手写（上面示例代码中的方式）
// 阶段 2：通过宏自动生成
#define REFLECT_COMPONENT(Name, ...) \
    static ComponentDesc get_desc() { return { #Name, { __VA_ARGS__ }, sizeof(Name) }; }

struct Transform {
    float x, y, z;
    REFLECT_COMPONENT(Transform,
        {"x", FieldType::Float, offsetof(Transform, x)},
        {"y", FieldType::Float, offsetof(Transform, y)},
        {"z", FieldType::Float, offsetof(Transform, z)}
    )
};
```

**C. `AgentBridge` 从 3 个工具扩展到标准化工具集**

| 工具名 | 作用 |
|--------|------|
| `query_entities(mask)` | 按组件掩码查询实体 |
| `get_component(e, name)` | 读取某个实体的某个组件字段（JSON 返回）|
| `set_component(e, name, json)` | 修改某个实体的某个组件字段 |
| `step(dt)` | 确定性推进一帧 |

---

### 阶段 2：好用 —— 解决"AI 能高效协作"

#### 触发原因

你开始让多个 Agent 并行工作：
- `PhysicsAgent` 负责调优碰撞参数
- `GameplayAgent` 负责调整敌人血量
- 但两个 Agent 同时请求修改状态，一个 Agent 的改动覆盖了另一个的结果。

同时，AI 的上下文窗口开始爆炸：每次对话都要 dump 整个世界的 10 万个实体状态。

#### 代码层面的变化

**A. 增量状态变更日志（Change Log）**

```cpp
struct ChangeLog {
    struct Entry {
        Entity e;
        std::string component;
        std::string field;
        std::string oldValue;
        std::string newValue;
    };
    std::vector<Entry> entries;
};

class World {
    ChangeLog changelog;
public:
    void notify_change(Entity e, const std::string& comp,
                       const std::string& field,
                       const std::string& oldVal,
                       const std::string& newVal) {
        changelog.entries.push_back({e, comp, field, oldVal, newVal});
    }
    const ChangeLog& get_changes() const { return changelog; }
    void clear_changes() { changelog.entries.clear(); }
};
```

AI 不需要读整个世界，只需要读这一帧的 `changelog`。

**B. 系统依赖图 + 分阶段 Tick**

```cpp
enum class Phase { PrePhysics, Physics, PostPhysics, Gameplay, Render };

class Scheduler {
    std::unordered_map<Phase, std::vector<std::unique_ptr<System>>> phases;
public:
    void add(Phase p, std::unique_ptr<System> sys) { phases[p].push_back(std::move(sys)); }
    void tick(World& world, float dt) {
        for (auto p : {Phase::PrePhysics, Phase::Physics, Phase::PostPhysics,
                       Phase::Gameplay, Phase::Render}) {
            for (auto& sys : phases[p]) {
                sys->tick(world, dt);
            }
        }
    }
};
```

**C. 世界快照与回放（Snapshot / Replay）**

```cpp
class WorldSnapshot {
    std::vector<char> rawData;  // 压缩后的组件数组 dump
public:
    static WorldSnapshot capture(const World& w);
    static void restore(World& w, const WorldSnapshot& snap);
};
```

Agent 的工作流变成：
1. `snapshot = capture(world)`
2. `bridge.set_component(...)` → 尝试改动
3. `bridge.step(dt)` → 运行几帧观察效果
4. 不满意？`restore(world, snapshot)` → 回滚到改动前

**D. 组件白名单与基础权限隔离**

```cpp
class AgentBridge {
    std::unordered_set<std::string> allowedComponents;
public:
    bool can_mutate(const std::string& comp) const {
        return allowedComponents.count(comp);
    }
};
```

---

### 阶段 3：工业级 —— 解决"规模、性能与 AI 自治"

#### 触发原因

- 实体数量从 100 增长到 10 万
- 你开始嵌入真正的认知型 NPC Agent
- 需要防止 AI 的"幻觉改动"破坏核心玩法逻辑

#### 代码层面的变化

**A. 从 `std::vector` 到 Archetype/Chunk 存储**

```cpp
struct Archetype {
    uint32_t componentMask;       // 位掩码表示包含哪些组件
    std::vector<Entity> entities; // 实体 ID 列表
    std::vector<std::vector<char>> columns; // 每列是一个组件类型的 SoA 数组
};
```

Archetype 不仅提升 cache locality，也对 AI 极其友好：
- AI 说"找出所有带 `Enemy` + `LowHealth` 的实体"，引擎直接定位到对应 Archetype，无需遍历整个世界。

**B. 确定性回放网络（Deterministic Replay Net）**

```cpp
struct FrameInput {
    uint32_t frameId;
    float fixedDt;
    std::vector<PlayerInput> playerInputs;
    uint64_t randomSeed;
};

class ReplayRecorder {
    std::vector<FrameInput> history;
public:
    void record(uint32_t frame, const FrameInput& input) { history.push_back(input); }
    bool replay(World& world, uint32_t targetFrame);  // 从第 0 帧重放到目标帧
};
```

这是自动化测试和 AI 自主调试的基石：给定相同的输入序列，世界状态必须比特级一致。

**C. 沙箱事务系统（Transactional Sandbox）**

```cpp
class TransactionalWorld {
    World mainWorld;
    World sandboxWorld;  // Agent 的改动先写这里
public:
    void enter_sandbox() { sandboxWorld = mainWorld; }
    void apply_to_main() { mainWorld = sandboxWorld; }  // 验证通过后原子提交
    void discard_sandbox() { /* 无需操作，下次 enter 时覆盖 */ }
};
```

**D. Orchestrator 层调度多 Agent**

```cpp
class AgentOrchestrator {
    struct AgentSpec {
        std::string name;
        std::vector<std::string> readComponents;
        std::vector<std::string> writeComponents;
    };
    std::vector<AgentSpec> agents;
public:
    void dispatch_all(AgentBridge& bridge);  // 按权限隔离并行调度 Agent
};
```

- `PhysicsAgent`：只读写 `RigidBody`、`Collider`
- `RenderAgent`：只读写 `Material`、`Mesh`
- `GameplayAgent`：只读写 `Health`、`Inventory`
- Orchestrator 负责合并它们的改动、检测冲突、决定执行顺序。

---

## 设计权衡表

| 决策点 | 原型阶段 | 商业引擎阶段 |
|--------|---------|-------------|
| ECS 存储 | 每个类型一个 `std::vector` | Archetype/Chunk-based 或 Sparse Set |
| 反射 | 宏 + 手动注册 | 编译器插件 / clang 解析生成 |
| AI 接口 | `AgentBridge` 手写 3~5 个工具 | 完整 MCP 协议 + Schema 验证 |
| 并发模型 | 单线程分阶段 Tick | 依赖图并行 + Job System |
| 状态变更可见性 | 全量轮询 | 增量 Change Log |
| 错误恢复 | 手动调试 | 快照 + 确定性回放 + 事务沙箱 |
| 渲染抽象 | 不需要（或最小 OpenGL 封装） | RHI + RenderGraph |
| 编辑器 | JSON Inspector 就够了 | 完整工具链 + MCP 桥接 |

> **关键洞察**：原型阶段的最大错误是过早引入商业引擎的复杂度；原型阶段欠下的技术债如果不包含"可观测性"和"确定性"，那么当你引入 AI 协作时，还债成本会指数级上升。

---

## 如果我要 vibe coding，该偷哪几招？

### 必偷（最小可行集）

1. **所有游戏状态必须是 ECS 组件**
   - 不要在脚本里藏局部变量，不要依赖隐式的单例状态。这是 AI 能看懂引擎的前提。

2. **暴露 `query_entities` + `get_component` 工具**
   - 让 AI 能用一句话拿到它关心的所有实体，并读取/修改具体字段。

3. **每一帧生成 `ChangeLog`**
   - 记录哪些实体的哪些组件被增删改，AI 只读变化的部分，节省上下文窗口。

4. **世界快照必须是廉价操作**
   - 使用 memory arena 或 copy-on-write，让 AI 能在几毫秒内"保存 → 尝试 → 回滚"。

5. **Tick 必须是确定性的**
   - 固定步长、固定随机种子、固定系统执行顺序。这是 AI 自主测试和回放的基石。

### 进阶偷（当项目超过原型规模）

6. **引入 Orchestrator 层调度多个专项 Agent**
   - 每个 Agent 绑定组件白名单，防止"越权操作"。

7. **Archetype 查询作为 AI 的第一类接口**
   - 让自然语言查询能直接映射到高效的 ECS archetype 迭代。

8. **构建 Deterministic Replay Pipeline**
   - 从 Day 1 就把所有输入按帧编号记录。未来 AI 可以自主跑回归测试。

9. **核心系统插入 Invariant 断言**
   - 让引擎本身成为 Agent 改动的"自动审核员"。

10. **为 AI 优化 Inspector 协议，而不是复用人类 GUI 协议**
    - 直接输出 JSON/Markdown，不要让 AI 去解析为人类设计的二进制 UI 数据。

---

## 延伸阅读

- [[AI友好的引擎架构]] —— 本知识库的设计宪法：可观测性、确定性、MCP 式工具边界与多 Agent 编排
- [[组件系统架构]] —— 从 OOP-Component-Based 到 Archetype ECS 的完整演进
- [[系统调度与确定性]] —— Tick 依赖图、并行调度、快照与事务沙箱
- [[MCP与Agent桥接层]] —— 引擎对外暴露标准化 AI 工具接口的工程实践

---

> [← 返回 SelfGameEngine 索引]([[00-索引|SelfGameEngine 索引]])

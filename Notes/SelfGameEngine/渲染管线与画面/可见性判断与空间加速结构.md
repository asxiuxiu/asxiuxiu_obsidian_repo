---
title: 可见性判断与空间加速结构
date: 2026-05-25
tags:
  - self-game-engine
  - rendering
  - culling
  - visibility
  - bvh
  - occlusion
aliases:
  - 可见性判断与空间加速结构
---

> **前置依赖**：[[ECS架构下的渲染世界设计]]
> **本模块增量**：掌握从逐实体视锥剔除到 GPU-Driven 剔除的完整技术栈。你将理解空间加速结构的构建与查询、HZB 遮挡剔除的原理，以及分阶段剔除策略的设计。
>
> 本笔记探讨的核心问题是：**场景有上万个物体，但相机只看见一小部分。如何以最小的 CPU 代价，精确找出"该被画"的物体集合？**

## 问题 3：如何判断一个实体"该不该被画"？

### 场景与根因

你的场景有 10000 棵树，但相机只朝向一小片森林。如果渲染管线处理全部 10000 棵树，即使最后通过 GPU 的视锥测试把它们都丢弃了，CPU 侧的 Draw Call 录制、材质绑定、矩阵上传仍然会发生——CPU 先崩溃了。

**根因在于：GPU 的背面剔除和视锥剔除发生在顶点着色器阶段，太晚了。** CPU 已经为了这些不可见物体付出了：遍历实体的开销、设置管线状态的开销、绑定资源的开销、写入命令缓冲的开销。对于 10000 个物体，CPU 开销可能是 5~10ms——帧预算的三分之一。

所以我们需要在**CPU 侧**做可见性判断，而且要在**尽可能早的阶段**做，避免为不可见物体做任何后续工作。

### 分支研究

#### 分支 A：逐实体暴力视锥测试

**核心思路**：对每个实体，用其包围盒（AABB 或包围球）与相机视锥的六个面做相交测试。在视锥内的保留，在外的丢弃。

```cpp
bool IsVisible(const AABB& bounds, const Frustum& frustum) {
    for (int i = 0; i < 6; ++i) {
        // 如果包围盒完全位于某一平面的背面，则不可见
        if (bounds.IsFullyBehind(frustum.planes[i]))
            return false;
    }
    return true;
}

// Extract 或 Queue 阶段
for (auto [mesh, transform] : renderables) {
    AABB world_bounds = transform.matrix * mesh.local_bounds;
    if (IsVisible(world_bounds, camera_frustum)) {
        visible_list.push_back(entity);
    }
}
```

**适用场景**：实体数量少（<1000）、实现简单优先。

**隐藏代价**：
- **O(n) 线性复杂度**。10000 个实体就要做 10000 次包围盒-视锥测试。虽然单次测试很快（几十个 CPU 周期），但总量累积不可忽视。
- **无遮挡剔除**。实体在视锥内，但可能被前面的建筑完全挡住。暴力视锥测试无法剔除这种情况。
- **无法利用空间局部性**。相邻的实体在 ECS 存储中可能分散在不同 Archetype 中，遍历顺序与空间位置无关，缓存不友好。

**失效条件**：实体数量 > 5000、或场景中有大量遮挡关系（如城市街道、室内房间）。

#### 分支 B：空间加速结构（BVH / Octree / 均匀网格）

**核心思路**：不再逐实体测试，而是先把实体组织成空间加速结构。测试时从根节点开始递归：如果整个节点的包围盒在视锥外，则跳过该节点及其所有子实体；如果在视锥内或与视锥相交，则继续递归到子节点。

```cpp
// 简化的 BVH 剔除
void CullBVH(Node* node, const Frustum& frustum, Array<Entity>* out) {
    if (!frustum.Intersects(node->bounds))
        return;  // 整个分支都不可见
    
    if (node->IsLeaf()) {
        for (Entity e : node->entities)
            out->push_back(e);
    } else {
        CullBVH(node->left, frustum, out);
        CullBVH(node->right, frustum, out);
    }
}
```

**适用场景**：静态物体占主体、场景空间大、需要遮挡剔除扩展的项目。

**隐藏代价**：
- **构建开销**。BVH 的构建不是免费的。对于静态场景可以预构建；对于动态物体（如角色、车辆），每帧更新 BVH 的代价可能超过遍历收益。
- **内存开销**。BVH 需要额外的节点存储。10 万实体的 BVH 可能有数万个节点。
- **动态物体处理**。动态物体通常不插入主 BVH，而是单独列表处理，这导致"静态 BVH 剔除 + 动态列表暴力测试"的双轨复杂度。
- **不适合 GPU 驱动**。BVH 遍历在 CPU 上是递归的，难以直接映射到 GPU 的并行执行模型。

**失效条件**：场景极度动态（如粒子系统、破坏效果）、或内存预算紧张无法容纳加速结构。

#### 分支 C：GPU-Driven 渲染——把剔除搬到 GPU

**核心思路**：CPU 只负责把**所有**物体的包围盒和变换矩阵上传到 GPU，由 Compute Shader 并行执行视锥剔除和遮挡剔除。剔除结果写入一个间接绘制缓冲区（Indirect Draw Buffer），然后 GPU 用 `DrawIndirect` 或 `MultiDrawIndirect` 直接消费这个缓冲区，无需 CPU 逐物体生成 Draw Call。

```cpp
// CPU 侧：只上传数据，不做剔除
void UploadCullingData(GpuBuffer instance_buffer, Array<InstanceData> instances) {
    instance_buffer->Upload(instances.data(), instances.size() * sizeof(InstanceData));
}

// GPU Compute Shader（概念性 HLSL）
[numthreads(64, 1, 1)]
void CullInstances(uint3 id : SV_DispatchThreadID) {
    uint instance_id = id.x;
    if (instance_id >= instance_count) return;
    
    InstanceData inst = instance_buffer[instance_id];
    AABB bounds = TransformAABB(inst.local_bounds, inst.world_matrix);
    
    if (!FrustumCull(bounds, view_frustum)) return;
    if (!OcclusionCull(bounds, hzb_texture)) return;  // HZB 遮挡剔除
    
    // 通过原子操作追加到可见列表
    uint slot;
    InterlockedAdd(visible_count[0], 1, slot);
    visible_instances[slot] = instance_id;
}

// 随后 Graphics Shader 通过 DrawIndirect 读取 visible_instances
```

**适用场景**：实体数量极大（>10万）、现代 GPU（D3D12/Vulkan）、CPU 录制 Draw Call 是明确瓶颈的项目。

**隐藏代价**：
- **需要 HZB（Hierarchical Z-Buffer）做遮挡剔除**。单纯的视锥剔除不够，GPU-driven 的优势需要配合 HZB 遮挡剔除才能充分发挥。HZB 的生成本身需要额外的 Pass。
- **间接绘制有平台限制**。旧版 OpenGL 不支持 `MultiDrawIndirect`；某些移动端 GPU 对间接绘制的支持有限。
- **调试困难**。如果某个物体莫名其妙消失了，问题可能出在 Compute Shader 的剔除逻辑、Instance Buffer 的数据、或 Indirect Buffer 的格式——CPU 调试器看不到 GPU 缓冲区的内容。
- **不适合材质差异大的场景**。`MultiDrawIndirect` 要求同一批次的物体共享相同的 PSO 和绑定组。如果 10 万个实体有 500 种不同的材质/管线，GPU-driven 的合批优势会被打破，需要复杂的 "mesh shader" 或 "work graph" 来重新组织。

**失效条件**：实体数量少（<5000）、目标平台不支持 Compute Shader 或间接绘制、或材质类型极其多样。

#### 分支 D：分阶段剔除——视锥 → HZB → 细节

**核心思路**：不追求"一次剔除解决所有问题"，而是把剔除分为多个精度递增的阶段：
1. **粗剔（CPU）**：用 BVH 或均匀网格快速剔除大量远距离/视锥外物体。
2. **中剔（GPU Compute）**：对粗剔保留的物体做精确的视锥剔除和 HZB 遮挡剔除。
3. **细剔（GPU Early-Z）**：在像素着色器之前利用深度预通道（Depth Prepass）的 Early-Z 剔除被遮挡的像素。

**适用场景**：大型开放世界、工业级引擎（UE5 Nanite、chaos 的 `GPUPrimitiveIndirectDrawer`）。

**隐藏代价**：
- **实现复杂度极高**。三个阶段需要精确的时序配合和数据流管理。
- **移动端不适用**。移动 GPU 的 tiled 架构对 Early-Z 的支持与桌面不同，Compute Shader 的功耗也可能较高。

**失效条件**：默认实现初期、团队规模小、没有专门的渲染程序员。

### 引擎对照

> **参考：chaos / UE / Bevy 对「可见性判断」是怎么做的？**
>
> - **chaos** 采用了**混合策略**：`RenderSceneManager::tick()` 在 `tickMain` 前被调用，负责更新可见性和裁剪结果。从源码推断，chaos 有 `GPUPrimitiveIndirectDrawer` 用于 GPU 驱动的间接绘制，说明它支持 GPU-driven 路径。但对于非 GPU-driven 的物体，它使用 CPU 侧的视锥剔除。chaos 的动机是：作为商业引擎，需要兼容从低端到高端的各种硬件，因此不能假设 GPU-driven 总是可用。
> - **UE** 采用了**多阶段剔除管线**：`FSceneRenderer::BeginInitViews` 执行 CPU 侧的视锥剔除（`FSceneView::ViewFrustum`）和遮挡剔除（`FOcclusionQuery` / HZB）。对于 Nanite 虚拟几何体，UE5 使用完全 GPU-driven 的簇光栅化，CPU 不触碰单个图元。对于传统静态网格，`FStaticMeshSceneProxy` 参与 CPU 剔除队列。UE 的动机是：不同渲染路径（Nanite vs 传统）需要不同的剔除策略，引擎提供统一的 `FViewInfo` 作为剔除结果的聚合容器。
> - **Bevy** 在 0.19 版本中**主要依赖 CPU 侧剔除**，但引入了 **GPU preprocessing** 作为优化路径。`queue_material_meshes` System 在 CPU 侧遍历实体，根据 `Aabb` 组件和相机视锥做视锥剔除，然后把可见实体加入 `BinnedRenderPhase`。对于支持 GPU preprocessing 的平台，Bevy 使用 Compute Shader 生成 instance buffer 和 indirect parameters，减少 CPU 端的逐实体处理。Bevy 的动机是：作为开源 ECS 引擎，它优先保证跨平台兼容性，GPU-driven 路径是 opt-in 优化而非默认路径。

### 决策分析与推荐

**默认推荐：分支 A + B 的渐进组合——阶段 5 初期用逐实体视锥测试，阶段 5 中期引入均匀网格或 BVH 加速，阶段 5 后期根据 profiling 结果评估 GPU-driven。**

理由如下：

1. **UE 的多阶段剔除证明了"没有银弹"**。UE 同时维护 CPU 剔除（传统网格）和 GPU-driven（Nanite）两条路径，因为不同内容类型适合不同策略。默认实现不应该一上来就追求 GPU-driven，而应该**先让 CPU 剔除跑通、可调试，再逐步迁移**。

2. **Bevy 的 CPU 视锥剔除是 ECS 下最自然的起点**。`Query<(&Aabb, &GlobalTransform)>` 直接遍历，用 `Aabb::intersects_frustum` 测试，代码量不超过 50 行。对于 <5000 实体的场景，这完全足够。

3. **BVH 对于静态场景是性价比极高的优化**。构建一次，每帧遍历。对于开放世界的地形、建筑等静态物体，BVH 可以将剔除复杂度从 O(n) 降到 O(log n)。推荐在阶段 5 中期引入，作为"看得见的效果"。

4. **GPU-driven 是长期演进方向，不是起步需求**。AMD 的 RPS SDK、Granite 引擎、ZetaRay 都展示了 GPU-driven 的威力，但它们的实现复杂度远超默认实现初期能承受的。关键是**在架构上预留 GPU-driven 的接口**——例如 `CullingSystem` 的输出是一个 `VisibleList` Resource，无论这个列表是 CPU 生成的还是 GPU 生成的，后续的 `Queue` 阶段都不需要改变。

**具体实施策略**：

```cpp
// 阶段 5 初期：逐实体视锥测试
void FrustumCullSystem(
    Query<(Entity, &Aabb, &GlobalTransform)> query,
    Res<Camera> camera,
    ResMut<VisibleList> visible
) {
    visible->clear();
    for (auto [e, aabb, transform] : query) {
        AABB world_aabb = aabb.Transform(transform.matrix);
        if (camera->frustum.Intersects(world_aabb)) {
            visible->push_back(e);
        }
    }
}

// 阶段 5 中期：用空间网格优化（概念）
void GridCullSystem(
    Res<SpatialGrid> grid,
    Res<Camera> camera,
    ResMut<VisibleList> visible
) {
    visible->clear();
    grid->ForEachCellIntersecting(camera->frustum, [&](Cell* cell) {
        for (Entity e : cell->entities) {
            // 细粒度测试
            if (IsVisible(e, camera->frustum))
                visible->push_back(e);
        }
    });
}
```

**关键设计点**：
- **剔除结果是一个独立的 ECS Resource**。`VisibleList` 或 `ViewVisibleEntities` 作为 Render World 的 Resource，被后续的 `Queue` 阶段消费。这让剔除策略可以独立演进——今天用 CPU 暴力测试，明天换成 BVH，后天换成 GPU Compute，下游系统无感知。
- **视锥数据来自 ExtractedCamera**。相机参数（位置、朝向、投影矩阵、视锥面）在 Extract 阶段从主 World 的 `Camera` 组件复制到 Render World 的 `ExtractedCamera` Resource。剔除系统只读 `ExtractedCamera`，不直接访问主 World。
- **AABB 组件是可选的**。没有 `Aabb` 的实体默认被视为"总是可见"（如天空盒、UI）。这避免了为不需要剔除的物体支付测试开销。

**遗留问题**：可见性判断后，我们得到了一份"应该被画的实体列表"。但直接对每个实体发一个 Draw Call 是灾难——1000 个实体就是 1000 次 Draw Call，现代 GPU 可以处理，但 CPU 录制 1000 次命令的开销会击穿帧预算。如何把这些实体组织成最少的 Draw Call，同时保证正确的绘制顺序（不透明从前到后？透明从远到近？）？



> **下一步**：[[渲染队列与DrawCall组织]]，因为可见性判断产出了一份"可见实体列表"。下一个问题是：如何把这些可见实体高效地组织成 GPU 命令？

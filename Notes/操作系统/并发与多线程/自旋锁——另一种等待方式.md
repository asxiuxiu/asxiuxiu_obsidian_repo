---
title: 自旋锁——另一种等待方式
date: 2026-05-01
tags:
  - 操作系统
  - 并发
  - 自旋锁
  - C++
aliases:
  - Spinlock
---

> [[索引|← 返回 并发与多线程索引]]

# 自旋锁——另一种等待方式

---

## 从一个问题开始：Mutex 在临界区很短时太"重"了

上一篇我们学了 `std::mutex`。它工作得很好，但当临界区很短时（比如就一句 `++counter`），Mutex 的代价反而成了瓶颈。

为什么？因为 Mutex 竞争失败时，操作系统会把线程**挂起（睡眠）**，切换到别的线程。等锁释放后再**唤醒**。一次上下文切换就是几微秒。如果临界区本身只要几十纳秒，你为了等这几十纳秒，付出了几千倍的代价。

这就像你等电梯——如果电梯马上就来（临界区很短），站在门口等比先去旁边咖啡店再回来更快。

**自旋锁（Spinlock）** 的思路就是："我就站在这儿等，不停敲门问'好了没？'"

---

## Spinlock：站着等

自旋锁的逻辑很简单：线程在循环中反复检查锁是否释放，**不放弃 CPU**。如果锁很快释放（下一个时钟周期就解了），自旋锁几乎没有额外开销。

用 `std::atomic` 可以实现一个简单的自旋锁：

```cpp
#include <atomic>
#include <thread>

class Spinlock {
    std::atomic<bool> flag{false};
public:
    void lock() {
        // exchange(true) 把 flag 设为 true，并返回旧值
        // 如果旧值是 false，说明抢到锁了；如果旧值是 true，说明锁被占了
        while (flag.exchange(true, std::memory_order_acquire)) {
            // 被占了，继续转……但别转太快，让 CPU 喘口气
            #if defined(__x86_64__) || defined(__i386__)
            __asm__ volatile("pause" ::: "memory");  // x86: 降低功耗，提示超线程
            #elif defined(__aarch64__)
            __asm__ volatile("yield" ::: "memory");  // ARM: 提示调度器
            #else
            std::this_thread::yield();  // 其他平台：让出 CPU
            #endif
        }
    }
    
    void unlock() {
        flag.store(false, std::memory_order_release);
    }
};
```

**关键代码解析**：

- `flag.exchange(true, acquire)`：这是**原子操作**。它把 `flag` 设为 `true`，同时返回原来的值。如果返回 `false`，说明之前没人持锁，你成功抢到了。如果返回 `true`，说明锁被占了。

- `while (...)`：抢不到就继续循环"自旋"。

- `pause` / `yield` 指令：纯粹自旋会疯狂占用 CPU，`pause` 告诉 CPU"我在忙等，可以优化功耗和流水线"，`yield` 则主动让出 CPU 给同核心的其他超线程。

---

## 什么时候用 Spinlock，什么时候用 Mutex？

| 场景 | 推荐 | 原因 |
|------|------|------|
| 临界区很短（几行代码） | Spinlock | 避免上下文切换开销 |
| 临界区长或竞争激烈 | Mutex | 自旋会浪费大量 CPU |
| 单核 CPU | Mutex | 自旋时占着 CPU，持锁线程没机会释放锁 |
| 需要睡眠（如等 IO） | Mutex | Spinlock 里不能睡眠 |

> **核心判断**：预期等待时间 < 两次上下文切换时间 → Spinlock；否则 → Mutex。

---

## 动手实验

### 实验：Mutex vs Spinlock 性能对比

```cpp
// mutex_vs_spinlock.cpp
#include <mutex>
#include <atomic>
#include <thread>
#include <vector>
#include <chrono>
#include <iostream>

constexpr int ITER = 1'000'000;
constexpr int NUM_THREADS = 4;

// 自旋锁实现
class Spinlock {
    std::atomic<bool> flag{false};
public:
    void lock() {
        while (flag.exchange(true, std::memory_order_acquire)) {
            #if defined(__x86_64__) || defined(__i386__)
            __asm__ volatile("pause" ::: "memory");
            #elif defined(__aarch64__)
            __asm__ volatile("yield" ::: "memory");
            #else
            std::this_thread::yield();
            #endif
        }
    }
    void unlock() { flag.store(false, std::memory_order_release); }
};

template<typename Lock>
void bench(const char* name) {
    Lock lk;
    int counter = 0;
    
    auto t1 = std::chrono::high_resolution_clock::now();
    std::vector<std::thread> threads;
    for (int i = 0; i < NUM_THREADS; ++i) {
        threads.emplace_back([&]() {
            for (int j = 0; j < ITER; ++j) {
                lk.lock();
                ++counter;  // 临界区：只有一行！
                lk.unlock();
            }
        });
    }
    for (auto& t : threads) t.join();
    auto t2 = std::chrono::high_resolution_clock::now();
    
    double ms = std::chrono::duration<double, std::milli>(t2 - t1).count();
    std::cout << name << ": " << ms << " ms (counter=" << counter << ")\n";
}

int main() {
    bench<std::mutex>("Mutex   ");
    bench<Spinlock>("Spinlock");
    return 0;
}
```

**编译运行**：
```bash
g++ -std=c++17 mutex_vs_spinlock.cpp -o mutex_vs_spinlock -pthread -O2
./mutex_vs_spinlock
```

**预期结果**（x86-64，4线程）：
- Mutex：通常 200~500 ms
- Spinlock：通常 100~300 ms

> [!info] 实验条件影响
> 线程数越多、竞争越激烈，Spinlock 的"站着等"浪费的 CPU 越多，Mutex 的优势越明显。上面的实验临界区只有一行 `++counter`，正是 Spinlock 的甜点区。

---

## 小结

| 特性 | Mutex | Spinlock |
|------|-------|----------|
| 等待方式 | 线程睡眠，不占用 CPU | 循环检查，占用 CPU |
| 获得锁时的开销 | 上下文切换（几微秒） | 几乎没有 |
| 适用场景 | 临界区长、竞争激烈 | 临界区短、竞争低 |
| 实现复杂度 | 简单（标准库直接提供） | 需自己实现或第三方库 |

> **问题链**：现在你有两种保护临界区的方式了。但无论是 Mutex 还是 Spinlock，消费者线程在队列为空时都在"干等"——Mutex 在反复加锁解锁，Spinlock 在疯狂自旋。能不能让线程"有任务了再叫醒我"？→ [[条件变量——有消息了叫你]]

---

> [!info] 延伸阅读
> - [[条件变量——有消息了叫你]] —— 避免忙等，有消息时叫醒线程
> - [[内存序——编译器和CPU如何"欺骗"你]] —— 深入理解 `acquire` 和 `release` 的含义

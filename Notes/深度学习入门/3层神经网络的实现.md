---
title: 3 层神经网络的实现
date: 2026-06-23
tags:
  - deep-learning
  - neural-network
  - forward-propagation
aliases:
  - 3 层神经网络的实现
---

> [[Notes/深度学习入门/Roadmap|← 返回 深度学习入门路线图]]

# 3 层神经网络的实现

知道了神经网络的结构和激活函数，下一步就是把它们组装起来，实现一个能从输入走到输出的完整网络。这个过程叫做**前向传播**（forward propagation）。

---

## 前向传播就是一层一层算

每一层的计算可以分成两步：

1. 用输入和权重做矩阵乘法，加上偏置，得到加权输入：
   $$
   \mathbf{A}^{(l)} = \mathbf{X} \mathbf{W}^{(l)} + \mathbf{B}^{(l)}
   $$
2. 把加权输入传给激活函数，得到这一层的输出：
   $$
   \mathbf{Z}^{(l)} = h(\mathbf{A}^{(l)})
   $$

下一层再把 $\mathbf{Z}^{(l)}$ 当作输入继续算，直到输出层。

---

## 用 NumPy 实现 3 层网络

假设输入是 2 个节点，第 1 隐藏层是 3 个节点，第 2 隐藏层是 2 个节点，输出层是 2 个节点：

```python
import numpy as np

def sigmoid(x):
    return 1 / (1 + np.exp(-x))

def identity_function(x):
    return x

def init_network():
    network = {}
    network['W1'] = np.array([[0.1, 0.3, 0.5], [0.2, 0.4, 0.6]])
    network['b1'] = np.array([0.1, 0.2, 0.3])
    network['W2'] = np.array([[0.1, 0.4], [0.2, 0.5], [0.3, 0.6]])
    network['b2'] = np.array([0.1, 0.2])
    network['W3'] = np.array([[0.1, 0.3], [0.2, 0.4]])
    network['b3'] = np.array([0.1, 0.2])
    return network

def forward(network, x):
    W1, W2, W3 = network['W1'], network['W2'], network['W3']
    b1, b2, b3 = network['b1'], network['b2'], network['b3']

    a1 = np.dot(x, W1) + b1
    z1 = sigmoid(a1)
    a2 = np.dot(z1, W2) + b2
    z2 = sigmoid(a2)
    a3 = np.dot(z2, W3) + b3
    y = identity_function(a3)

    return y

network = init_network()
x = np.array([1.0, 0.5])
y = forward(network, x)
print(y)  # 输出两个数值
```

这里的 `identity_function` 是**恒等函数**，直接返回输入。它常用于回归问题的输出层。

---

## 输出层的设计

输出层用什么函数，取决于任务类型：

- **回归问题**：输出是连续值，用**恒等函数**。
- **分类问题**：输出是概率分布，用 **softmax 函数**。

Softmax 的公式是：

$$
y_k = \frac{\exp(a_k)}{\sum_{i=1}^{n} \exp(a_i)}
$$

它把输出层的任意实数变成概率，所有输出之和为 1。

```python
import numpy as np

def softmax(a):
    c = np.max(a)  # 防止指数爆炸
    exp_a = np.exp(a - c)
    sum_exp_a = np.sum(exp_a)
    return exp_a / sum_exp_a

a = np.array([0.3, 2.9, 4.0])
print(softmax(a))  # [0.018 0.245 0.737]
print(np.sum(softmax(a)))  # 1.0
```

注意代码里先减去了 `np.max(a)`。这是因为 $\exp$ 对大的数会溢出，减去最大值不影响结果（分子分母同时约掉一个常数），但能避免数值问题。

---

## 批处理：一次推理多张图片

实际应用时，我们不会一张一张地输入图片，而是把多张图片打包成一个矩阵一起算。这种打包好的数据叫一个 **batch**（批次）。

比如 MNIST 数据集中，一张图片是 784 维向量。如果一次处理 100 张，输入就是一个 `(100, 784)` 的矩阵：

```python
import numpy as np

# 假设 network 已经初始化，输入 x 是 (100, 784) 的 batch
x = np.random.rand(100, 784)

W1 = network['W1']  # (784, 50)
b1 = network['b1']  # (50,)
W2 = network['W2']  # (50, 100)
b2 = network['b2']  # (100,)
W3 = network['W3']  # (100, 10)
b3 = network['b3']  # (10,)

a1 = np.dot(x, W1) + b1      # (100, 50)
z1 = sigmoid(a1)
a2 = np.dot(z1, W2) + b2     # (100, 100)
z2 = sigmoid(a2)
a3 = np.dot(z2, W3) + b3     # (100, 10)
y = softmax(a3)              # 每行是一个样本的概率分布
```

通过广播，`b1` 会自动加到 `a1` 的每一行上。输出 `y` 的形状是 `(100, 10)`，每一行对应一张图片属于 0~9 的概率。

---

## 对 MNIST 进行推理

把以上代码组合起来，就可以对 MNIST 手写数字进行推理。书中的完整流程是：

1. 加载 MNIST 数据集和预训练权重（通常是 `sample_weight.pkl`）。
2. 把每张图片的像素值归一化到 0~1 之间。
3. 用 3 层网络做前向传播，输出 softmax 概率。
4. 取概率最大的类别作为预测结果。

```python
import numpy as np

def predict(network, x):
    W1, W2, W3 = network['W1'], network['W2'], network['W3']
    b1, b2, b3 = network['b1'], network['b2'], network['b3']

    a1 = np.dot(x, W1) + b1
    z1 = sigmoid(a1)
    a2 = np.dot(z1, W2) + b2
    z2 = sigmoid(a2)
    a3 = np.dot(z2, W3) + b3
    y = softmax(a3)
    return y
```

> 注意：上面的代码需要配合 MNIST 数据集和预训练权重才能实际运行。这里的关键是理解前向传播的流程。

---

## 小结

- **前向传播**就是从输入层逐层计算到输出层的过程。
- 每一层都是 $\mathbf{A} = \mathbf{XW} + \mathbf{B}$ 加上激活函数。
- 输出层根据任务选择函数：回归用恒等函数，分类用 softmax。
- **批处理**能一次性推理多个样本，利用矩阵运算提高效率。
- 3 层神经网络的前向传播代码是后面实现训练循环的基础。

---

> [[Notes/深度学习入门/Roadmap|← 返回 深度学习入门路线图]]
